{
  "name": "mcp-drone-ci",
  "version": "1.0.0",
  "description": "MCP server scaffold to monitor and react to Drone CI pipelines.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.3.2",
    "typescript": "^5.5.3"
  },
  "engines": {
    "node": ">=20"
  },
  "private": false
}
